group 'me.minidigger'
version '1.0-SNAPSHOT'

dependencies {
    compileOnly rootProject
}

task createPom() {
    pom {
        project {
            groupId 'com.voxelgameslib'
            artifactId 'chatmenuapi'
            version version
        }
    }.writeTo("pom.xml")
}