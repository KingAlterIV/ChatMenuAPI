group 'me.minidigger'
version '1.0-SNAPSHOT'

task createPom() {
    pom {
        project {
            groupId 'com.voxelgameslib'
            artifactId 'chatmenuapi'
            version version
        }
    }.writeTo("pom.xml")
}